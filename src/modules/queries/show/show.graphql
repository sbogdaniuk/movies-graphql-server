type Show @cacheControl(maxAge: 240) {
  id: ID!
  name: String!
  type: String
  language: String
  genres: [String]!
  image: Image!
  summary: String
  a: String
}

type Image @cacheControl(maxAge: 1000) {
  medium: String
  original: String
}

extend type Query {
  show(id: ID!): Show
}
